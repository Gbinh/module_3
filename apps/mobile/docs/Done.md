# Done — Archived features

> Feature user verify OK (`[x] Verified`) → **AI auto-move** từ `business.md` sang đây,
> thêm `Ngày xong`. Giữ nguyên số `FEATURE #N` — không reuse cho feature mới.
>
> Không sửa content — nếu cần revise, tạo FEATURE mới trong `business.md` với số kế tiếp.
>
> File này ban đầu rỗng — feature verified sẽ tự thêm vào theo format:
>
> ```md
> ## FEATURE #N: <tên feature>
>
> **Status**: `[x] Verified`
> **Ngày tạo**: `<YYYY-MM-DD>`
> **Ngày xong**: `<YYYY-MM-DD>`
>
> ### Tôi muốn / Request backend ở / Out of scope / Verify steps
> ```
